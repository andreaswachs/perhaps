# Docker development Procfile - web container only (Sidekiq runs in separate container)
web: bundle exec ${DEBUG:+rdbg -O -n -c --} bin/rails server -b 0.0.0.0 -p 3000
css: bundle exec bin/rails tailwindcss:watch 2>/dev/null
